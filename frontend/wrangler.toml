name = "baakbook-pages-local"
pages_build_output_dir = "."
compatibility_date = "2026-08-20"

# Production and preview variables are configured in the Cloudflare Pages dashboard.
# Local emulator values belong in .dev.vars and must never be deployed.
