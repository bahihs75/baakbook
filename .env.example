# Secret used by the /admin API. Set a long random value in the hosting dashboard.
ADMIN_TOKEN=replace-with-a-long-random-secret

# Optional path to the writable runtime data file.
# On PythonAnywhere or Render, use a path appropriate to the deployed service.
DATA_FILE=data.json
