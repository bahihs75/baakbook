# نتائج المعاينة المحلية — 2026-08-20

- تم تشغيل Pages محليًا على `http://127.0.0.1:8788/`.
- بعد إضافة `frontend/.dev.vars` الذي يحتوي على `FIREBASE_PROJECT_ID=baakbook-77c00`، نجحت مسارات القراءة العامة من Firestore الإنتاجي.
- ظهرت 22 بطاقة كتاب في الصفحة الرئيسية، مع تصنيفات «روايات» و«تنمية بشرية» وتصنيف «كتب يمكن اقتناؤها».
- رسالة «تعذر تحميل البيانات من الخادم» في المحاولة الأولى كانت بسبب غياب متغير `FIREBASE_PROJECT_ID` من بيئة Wrangler المحلية، وليست بسبب كود الواجهة.
- فحص `npm run build` وفحص تركيب وحدتي JavaScript في `admin.html` و`index.html` نجحا.
- فحص صياغة `firestore.rules` عبر محاكي Firestore المحلي نجح.
- لم يحدث نشر، أو `git push`، أو تعديل في PythonAnywhere، أو تعديل مباشر في Firestore أثناء هذه المعاينة.
